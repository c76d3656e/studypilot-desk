{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions required by the StudyPilot desktop window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
